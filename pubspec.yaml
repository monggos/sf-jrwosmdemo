name: sf_jrwosm
description: "JRW OpenStreetMap - Spice Factory Mobile Application (Flutter)"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  latlong2: ^0.9.1
  flutter_map: ^7.0.0
  flutter_map_geojson: ^1.0.8
  flutter_map_cancellable_tile_provider: ^3.0.0-dev.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true